# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# Editor
*.swp
*.swo
*~
.vscode/
.idea/

# Local environment
.env
.env.local
.env.*.local

# Logs
*.log

# Backups y temporales
*.bak
*.tmp
tmp/
.cache/

# Por si se añade tooling más adelante
node_modules/
dist/
build/

# Espacio personal del usuario (no trackeado): notas crudas, drafts privados
private/
scratch/

# El repo de la herramienta no acumula contenido. Cualquier `audience-ops/` que
# se cree aquí por testing local debe ignorarse (vive en repos host, no aquí).
audience-ops/
